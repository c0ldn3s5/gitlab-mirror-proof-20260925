stages:
  - test

canary_check:
  stage: test
  image: alpine:3.20
  script:
    - |
      if [ -n "${RESEARCH_CANARY:-}" ]; then
        printf 'CANARY_SHA256='
        printf '%s' "$RESEARCH_CANARY" | sha256sum | cut -d' ' -f1
      else
        echo CANARY_ABSENT
      fi
